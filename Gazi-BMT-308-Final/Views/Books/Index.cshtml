@model IEnumerable<Gazi_BMT_308_Final.Models.Book>

<h2>Kitaplar</h2>

<form asp-controller="Books" asp-action="Index" method="get">
    <div class="form-group">
        <input type="text" class="form-control" id="SearchString" name="SearchString" placeholder="Kitap veya Yazar adı ile ara">
        <button type="submit" class="btn btn-primary">Ara</button>
    </div>
</form>

<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Başlık</th>
            <th>Yazar</th>
            <th>Yayım Yılı</th>
            <th>İşlemler</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var book in Model)
        {
            <tr>
                <td>@book.Id</td>
                <td>@book.Title</td>
                <td>@book.Author</td>
                <td>@book.PublicationYear</td>
                <td>
                    @if (User.IsInRole("Admin"))
                    {
                        <a asp-action="Edit" asp-controller="Books" asp-route-id="@book.Id" class="btn btn-warning">Düzenle</a>
                        <a asp-action="Delete" asp-controller="Books" asp-route-id="@book.Id" class="btn btn-danger">Sil</a>
                    }
                    <button class="btn btn-primary">Ödünç Al</button>
                    <button class="btn btn-info">İade Et</button>
                </td>
            </tr>
        }
    </tbody>
</table>

